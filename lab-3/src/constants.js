const START = 'A';
const STOP = 'B';
const OBSTACLE = 'X';
const EMPTY = ' ';
const PLAYER = 'P';

const OBSTACLE_RATIO = 0.25;

module.exports = { START, STOP, OBSTACLE, EMPTY, PLAYER, OBSTACLE_RATIO };
